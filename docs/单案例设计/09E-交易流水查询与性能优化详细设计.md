# 案例E - 交易流水查询与性能优化详细设计

## 1. 文档目的

本文档用于指导 AI 在当前工程中新增“交易流水查询接口”，并按企业级查询接口要求完成分页、条件过滤、排序和性能优化设计。

实现本案例前，AI 必须先读取：
- `docs/09-00-全局约束与工程基线.md`

建议先完成：
- `docs/09A-账户入金接口详细设计.md`
- `docs/09B-行内转账与幂等控制详细设计.md`

---

## 2. 目标

在当前工程基础上实现：
1. 交易流水查询接口
2. 按 accountNo 查询账户相关流水
3. 支持 txnType、startTime、endTime 过滤
4. 支持分页
5. 按 txnTime 倒序
6. 返回当前账户视角的方向、对手账号和余额快照
7. 避免低质量查询实现（如全量查出后内存过滤）

---

## 3. 业务规则

1. `accountNo` 必填
2. `accountNo` 必须存在
3. `startTime` 和 `endTime` 若同时存在，则必须满足 `startTime <= endTime`
4. `pageNo` 默认 1
5. `pageSize` 默认 20，最大 100
6. 返回当前账户作为借方或贷方的所有交易
7. 默认按 `txnTime desc` 排序
8. 入金交易允许无对手账号
9. 返回结果中必须计算当前账户视角的：
    - `direction`
    - `counterpartyAccountNo`
    - `balanceAfter`

---

## 4. 涉及文件

## 4.1 新增文件
- `src/main/java/com/bank/poc/dto/request/TransactionQueryRequest.java`
- `src/main/java/com/bank/poc/dto/response/TransactionItemResponse.java`
- `src/main/java/com/bank/poc/dto/response/TransactionQueryResponse.java`

## 4.2 修改文件
- `src/main/java/com/bank/poc/controller/TransactionController.java`
- `src/main/java/com/bank/poc/service/TransactionService.java`
- `src/main/java/com/bank/poc/service/impl/TransactionServiceImpl.java`
- `src/main/java/com/bank/poc/repository/TransactionRepository.java`
- `src/main/resources/schema.sql`

---

## 5. DTO 设计

## 5.1 TransactionQueryRequest

### 字段
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| accountNo | String | 是 | 查询账户号 |
| txnType | String | 否 | 交易类型 |
| startTime | LocalDateTime | 否 | 开始时间 |
| endTime | LocalDateTime | 否 | 结束时间 |
| pageNo | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页大小，默认20 |

### 校验要求
- `accountNo`：`@NotBlank`
- `pageNo`：若为空则默认1
- `pageSize`：若为空则默认20，最大100

### 时间绑定建议
在 Controller 层使用：
```java
@ModelAttribute
```
并结合：
```java
@DateTimeFormat(iso = DateTimeFormat.ISO.DATE_TIME)
```

---

## 5.2 TransactionItemResponse

### 字段
| 字段 | 类型 | 说明 |
|---|---|---|
| txnId | String | 交易流水号 |
| requestId | String | 请求唯一号 |
| direction | String | DEBIT / CREDIT |
| counterpartyAccountNo | String | 对手账号 |
| txnType | String | 交易类型 |
| amount | BigDecimal | 金额 |
| txnStatus | String | 交易状态 |
| remark | String | 备注 |
| txnTime | LocalDateTime | 交易时间 |
| balanceAfter | BigDecimal | 当前账户视角下交易后余额 |

---

## 5.3 TransactionQueryResponse

### 字段
| 字段 | 类型 | 说明 |
|---|---|---|
| total | long | 总记录数 |
| pageNo | Integer | 页码 |
| pageSize | Integer | 每页大小 |
| records | List<TransactionItemResponse> | 明细列表 |

---

## 6. Controller 设计

## 6.1 修改类
`TransactionController`

## 6.2 新增方法
```java
@GetMapping
public ApiResponse<TransactionQueryResponse> queryTransactions(@Valid @ModelAttribute TransactionQueryRequest request)
```

### 职责
- 参数绑定
- 参数校验
- 调用 `transactionService.queryTransactions(request)`
- 返回 `ApiResponse.success(...)`

---

## 7. Service 设计

## 7.1 TransactionService
新增方法：
```java
TransactionQueryResponse queryTransactions(TransactionQueryRequest request);
```

---

## 7.2 TransactionServiceImpl#queryTransactions 详细流程

### 方法签名
```java
public TransactionQueryResponse queryTransactions(TransactionQueryRequest request)
```

### 处理流程
1. 校验 `accountNo` 必填
2. 查询账户是否存在，不存在抛 `ACCT404`
3. 初始化分页参数：
    - `pageNo = request.pageNo == null || request.pageNo < 1 ? 1 : request.pageNo`
    - `pageSize = request.pageSize == null || request.pageSize < 1 ? 20 : request.pageSize`
    - 若 `pageSize > 100`，取 100 或抛异常
    - 推荐：直接限制为 100
4. 校验时间范围：
    - 若 `startTime != null && endTime != null && startTime.isAfter(endTime)`，抛 `TXN002`
5. 构造 `Specification<TransactionEntity>`：
    - `(debitAccountNo = accountNo or creditAccountNo = accountNo)`
    - 若 `txnType` 非空，则增加 `txnType = request.txnType`
    - 若 `startTime` 非空，则 `txnTime >= startTime`
    - 若 `endTime` 非空，则 `txnTime <= endTime`
6. 构造分页对象：
    - `PageRequest.of(pageNo - 1, pageSize, Sort.by(Sort.Direction.DESC, "txnTime"))`
7. 调用 `transactionRepository.findAll(specification, pageable)`
8. 将 `Page<TransactionEntity>` 转换为 `TransactionQueryResponse`

---

## 8. 结果转换规则

对每一条 `TransactionEntity`，根据当前 `accountNo` 决定返回视角：

### 8.1 若当前账户为借方
条件：
```text
accountNo == transaction.debitAccountNo
```

返回：
- `direction = "DEBIT"`
- `counterpartyAccountNo = transaction.creditAccountNo`
- `balanceAfter = transaction.debitBalanceAfter`

### 8.2 若当前账户为贷方
条件：
```text
accountNo == transaction.creditAccountNo
```

返回：
- `direction = "CREDIT"`
- `counterpartyAccountNo = transaction.debitAccountNo`
- `balanceAfter = transaction.creditBalanceAfter`

### 8.3 若是入金交易
由于 `debitAccountNo = null`，当前账户必为贷方：
- `direction = "CREDIT"`
- `counterpartyAccountNo = null`
- `balanceAfter = creditBalanceAfter`

---

## 9. Repository 设计

## 9.1 TransactionRepository
必须支持：
```java
public interface TransactionRepository extends JpaRepository<TransactionEntity, String>, JpaSpecificationExecutor<TransactionEntity>
```

说明：
- 不允许仅用 `findAll()` 然后在 Java 内存中过滤
- 必须走数据库分页与条件过滤

---

## 10. schema.sql 优化要求

建议补充以下索引：

```sql
CREATE INDEX IF NOT EXISTS idx_transaction_debit_account_no
    ON t_transaction(debit_account_no);

CREATE INDEX IF NOT EXISTS idx_transaction_credit_account_no
    ON t_transaction(credit_account_no);

CREATE INDEX IF NOT EXISTS idx_transaction_txn_time
    ON t_transaction(txn_time);

CREATE INDEX IF NOT EXISTS idx_transaction_txn_type
    ON t_transaction(txn_type);
```

---

## 11. 性能优化要求

AI 在实现本接口时必须遵循以下要求：

1. 不允许 `findAll()` 后在内存中过滤
2. 必须使用数据库分页
3. 必须支持条件过滤
4. 必须按 `txnTime DESC` 排序
5. 必须保持响应结构稳定
6. 必须给出或落实索引增强

---

## 12. 测试场景

1. 查询某账户所有流水
2. 查询入金流水
3. 查询转账流水
4. 按 `txnType` 过滤
5. 按时间范围过滤
6. 分页查询
7. `pageSize > 100`
8. `startTime > endTime`
9. 查询不存在账户

---

## 13. AI 输出要求

AI 必须：
1. 输出完整 DTO、Controller、Service、Repository 修改代码
2. 使用 `JpaSpecificationExecutor`
3. 保持当前工程风格
4. 不得用低质量查询方式替代
```