<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bank.poc.mapper.ProductMapper">

    <select id="selectByProductCode" resultType="ProductEntity">
        SELECT
        product_code,
        product_name,
        product_type,
        currency,
        sale_status,
        account_level,
        created_time,
        updated_time
        FROM t_product
        WHERE product_code = #{productCode}
    </select>

    <select id="selectBySaleStatus" resultType="ProductEntity">
        SELECT
        product_code,
        product_name,
        product_type,
        currency,
        sale_status,
        account_level,
        created_time,
        updated_time
        FROM t_product
        WHERE sale_status = #{saleStatus}
        ORDER BY product_code
    </select>

</mapper>